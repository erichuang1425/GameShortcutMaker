[pytest]
# The modules under test live at the repo root (versioning.py, scanner.py, ...).
# pythonpath puts the root on sys.path so `pytest tests/` works the same as
# `python -m pytest` (which implicitly adds the CWD).
pythonpath = .
testpaths = tests
